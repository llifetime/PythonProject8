[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "myproject"
version = "0.1.0"
dependencies = [
    "Django==4.2.7",
    "Pillow==10.0.0",
]

[tool.black]
line-length = 88
target-version = ['py310']